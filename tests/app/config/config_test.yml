coverd_money:
    currencies: ["USD", "EUR", "CAD"]
    reference_currency: "EUR"
    decimals: 3

framework:
    trusted_hosts: ~
    secret: "test"
    test: ~

doctrine:
    dbal:
        connections:
            default:
                driver:   "pdo_sqlite"
                memory:   true
                path:     ":memory:"
            other:
                driver:   "pdo_sqlite"
                memory:   true
                path:     ":memory:"

    orm:
        entity_managers:
            default:
                auto_mapping: true
            other:
                connection: other
